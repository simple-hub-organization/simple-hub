json_key_file(
  if ENV["GITHUB_ACTIONS"] == "true"
    "/tmp/google_play_key.json"
  else
    "../../../../../Smart-home-passwords/simple_hub/googlePlayStore/google_play_key.json"
  end
)

package_name("com.mysimplehub")
